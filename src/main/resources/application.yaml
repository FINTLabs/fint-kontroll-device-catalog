fint:
  org-id: fintlabs.no
  application-id: fint-kontroll-device-catalog
  kontroll:
    publishing:
      initial-delay: 100000
      fixed-delay: 300000

novari:
  cache:
    defaultCacheEntryTimeToLive: "20000ms"
    defaultCacheHeapSize: 10000

spring:
  application:
    name: fint-kontroll-device-catalog
  kafka:
    topic:
      org-id: fintlabs-no                 # TODO: MÅ BORT NÅR DEN REGISTRERES I KUSTOMIZE OVERLAY
      domain-context: kontroll
    application-id: fintlabs.no
    default-replicas: 1
  flyway:
    enabled: true
    locations: classpath:db/migration
  profiles:
    include:
      - db
      - observability
logging:
  level:
    root: INFO
    org.apache.kafka.clients: WARN
    org.apache.kafka.common: WARN
    org.apache.kafka.clients.consumer: WARN
    org.apache.kafka.clients.producer: WARN
    org.springframework.kafka: WARN
    no.fintlabs.kafka: WARN
    no.fintlabs.cache.ehcache.FintEhCacheEventLogger: WARN

